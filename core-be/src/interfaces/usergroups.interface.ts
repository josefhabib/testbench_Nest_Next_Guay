// Create an interface for the UserGroups object (make take the following values: 'admin', 'clinician', 'researcher')

export interface IUserGroups {
  admin: 'admin';
  clinician: 'clinician';
  researcher: 'researcher';
}